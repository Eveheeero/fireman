use super::PE;

impl PE {
    pub(super) fn _parse_from_virtual_address(
        &self,
        address: u64,
    ) -> Result<(), Box<dyn std::error::Error>> {
        todo!();
        Ok(())
    }
}
