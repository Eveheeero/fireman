# Absolute value recovery: (x < 0) ? -x : x → abs(x).

if:
  at afterIteration
  expr Ternary(BinaryOp(Less, $x, Literal(Int(0))), UnaryOp(Negate, $x2), $x3)
  where structurally_equal($x, $x2)
  where structurally_equal($x, $x3)
do:
  replace_expr Call("abs", [$x])
